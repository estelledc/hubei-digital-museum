# 项目阶段收尾与后续计划

本仓负责公开里程碑，日常开发已转入相邻的非 public 项目，见[开发分工](DEVELOPMENT.md)。本次阶段已收尾。用户已整体确认[下一阶段计划](#已确认的下一阶段计划精品路线与三组核心文物)，明确后续再来继续制作。已发布版本与本地试制状态分别保留；下次从[接续入口](#从哪里继续)的 N-01 开始。

- [x] 确认发布范围：estelledc/hubei-digital-museum，GitHub Pages。
- [x] 独立整理公开目录，保留本地完整版。
- [x] 删除未确认再分发许可的照片、VR 图像及其模型贴图，保留来源链接和开放许可署名。
- [x] 将网页导出为静态站，支持仓库子路径；无损压缩模型。
- [x] 验证模型交互、静态构建、公开文件和资源路径。
- [x] 创建公开 GitHub 仓库、推送并启用 Pages；验证线上页面及资源。

已发布：[公开仓库](https://github.com/estelledc/hubei-digital-museum)、[GitHub Pages](https://estelledc.github.io/hubei-digital-museum/)、[Blender 下载](https://github.com/estelledc/hubei-digital-museum/releases/tag/v0.13.0)。

## 本轮收尾：文物覆盖盘点与后续计划

范围：核对当前公开资产与馆方公开目录，整理本项目尚未建模的候选及已有模型的精度缺口；本轮交付研究和计划，不新增模型。

- [x] 核对现有 16 组文物与展陈模型及对应实际资源。
- [x] 查阅馆方公开资料，记录缺项、来源、资料不足与数字资源状态。
- [x] 制定分批建模、交互、版权和验收计划，并从 README 链接。

交付正文：[文物覆盖盘点与后续数字化计划](research/digitization-roadmap.md)。共 32 项新增候选（首批 6 项、后续 26 项），已有模型的欠缺另列。原盘点已由 `5785d52` 提交并推送；后续制作进度见下。

交付门：检查文档及引用，以一个文档 commit 推送到 `main`，随后核对远端提交和 Pages 工作流。GitHub 的实际提交与工作流结果作为发布状态，不提前将后续建模标为完成。

## 高质量模型制作与部署（2026-09-08 启动，本次留待后续）

用户此前要求执行上述计划并强调模型质量；本次明确先以已确认的后续计划收尾，制作留待后续。完整范围仍是 32 项新增候选、16 组既有模型精修，以及计划 E 的展区定名与目录补查。下列保留历史制作进度；恢复时优先按本文件已确认的精品路线推进，不直接重启原清单的扩批顺序。

- [x] 检查公开工作树及既有交互基线；保留已发布版本。
- [x] 查明两件试制文物身份与首批照片来源。
- [x] 完成 P1 六项首轮资料试查，记录头骨既有扫描、漆奁载体、帽顶尺寸冲突及各项待补材料。
- [ ] 制作 N09 鸳鸯形盒：按多角度照片校准器形，保留两侧漆绘差异，建立头颈转动与独立盖体。
- [x] 为 N09 接入本地网页动作控制，验证单次／循环、拖动、暂停、复位与减少动态效果。
- [ ] 恢复后核对浏览器可用性并完成原路径验收；此前自动化受 Mac 锁屏阻止，本次未重试。
- [ ] 补齐 N29 斗彩碗的环向、内底和底款资料；目前不满足高质量表面制作输入。
- [ ] 交付两件试制的 Blender 工程、网页模型和四面／局部对照，验证交互并取得视觉接受。
- [ ] 依原计划完成 P1 其余四项、已有模型精修、P2 的 26 项及 E 阶段。
- [ ] 更新来源、注册表、Release，完成网页检查并验证 GitHub Pages 部署。

逐项制作状态与证据见 [模型制作记录](research/model-production.md)。

## 后续计划共同讨论

本节是本次 Shape Record，继续维护在现有任务入口中。用户于 2026-09-08 调用 `shape-task`，要求共同讨论后续计划。后续目标、方案和验收现已获用户整体确认；用户要求将计划作为本次项目收尾，后续再继续制作。模型制作保持当前状态，之前已发布版本与未提交试制文件均保留。本轮完成计划文档；用户随后要求放入项目并收尾，现按此前计划 commit／push 授权交付文档。模型制作与新模型发布仍留待后续。

### 已有约定与现在的问题

已有明确选择继续有效：长期做现馆全展区与各厅代表文物；以现馆为依据，缺资料处允许近似并标明；提供可编辑 Blender 模型，能交互的加入交互，复杂结构可用明确说明的爆炸展示；公开仓库与 GitHub Pages 已建立。本轮不重复询问这些事项。

用户已明确选择：下一阶段优先完成一条从入口、公共空间到代表展厅及核心文物的精品参观路线。32 项新增候选与 16 组精修继续作为长期工作清单，其他区域不要求在同一阶段同时达标。用户进一步选择局部漫游：跨区域从导览点切换，区域内提供合适的推荐观看位置，也允许自主观察。用户已接受首阶段把高精度近看与完整交互集中于 2–3 件核心文物。三个承重决定及下文完整约定均已确认；本次完成计划与接续记录，实施等待用户后续继续。

本轮重新检查工作树与制作记录：HEAD 为 `5785d52`，本地仍有未提交的鸳鸯盒模型、交互代码和资料更新。制作记录中的 N09 是本地试制，尚未通过用户视觉接受；P1 其余五项仍存在多面资料、身份、尺寸或素材缺口。这里引用的是当前项目记录，没有重新宣称全馆视觉检查通过。各项事实和来源唯一维护在 [模型制作记录](research/model-production.md)，不在本节复制素材清单。

### 本轮候选与人类决定

**D-NEXT-01：下一阶段优先验收的访客体验。** 状态 `ACCEPTED`；用户回复“我觉得 B 没问题”，接受下列 B。绑定合同的 outcome 与 scope；这次回答确认阶段优先级，不代表具体路线、移动方式、质量标准或整体合同也已确认。

- A：先让少数代表文物经得起放大、旋转和交互近看。优先验证单件质量；全馆空间体验暂时变化较少。
- B：先完成一条从入口、公共空间到代表展厅及核心文物的精品参观路线。优先验证完整游览体验；其他区域暂不同时达标。
- C：先让全馆展区和代表文物覆盖更完整，再逐件精修。优先验证覆盖；需要接受下一版中各处精度仍有差异。

当时推荐 B 的理由是用户已多次要求优化全馆观感和四个公共空间，完整路线可以同时观察环境、导览、文物近看与交互是否形成一致体验。用户现已接受。最强反方仍需在方案中处理：路线本身可能过宽，让单件精度再次被摊薄；失败情境是新增了一批文物，访客走到展柜并放大后仍看到拉伸贴图和粗糙轮廓，原有“玩具感”没有改善。A、C 保留为当时比较过的阶段优先级，不据此取消长期全馆目标。

**D-NEXT-02：首阶段的近看与完整交互需要覆盖哪些文物。** 状态 `ACCEPTED`；用户对上一轮“先集中做好 2–3 件核心文物”的具体问题回复“可以的”；前置 D-NEXT-01 与 D-NEXT-03 均已接受；绑定 scope 与 acceptance criteria。R-03 将原来的抽象质量题收敛为具体覆盖取舍：已有真实感偏好与最新自由观察要求足以提出空间质量底线，不再要求用户选择面数、材质算法或重复回答“是否要真实”。用户现已选择将首阶段文物独立近看与完整交互集中于少数核心对象。具体名单在整体合同中列明。

整体合同已确认的验收要求：推荐视角和自由观察使用同一套场景；可达位置所见的正面、背面与侧面都要完整，走近后不能出现明显的穿模、贴图拉伸或失真的轮廓。空间主色、材料、比例与关键陈设按可核实实景对照；缺资料的近似明确标注。核心文物须能观察形制、纹饰、材质和有依据的动作。验收同时包含预设镜头、离开预设位置后的自由观察、文物细看与动作前后；用户视觉接受单独记录，测试通过不代替接受。具体停止条件见已确认合同。

已接受的覆盖取舍：首阶段先完成 2–3 件核心文物的独立近看与完整交互，编钟、编磬此前是举例候选；整条路线空间及沿途陈列仍须在实际可达观看距离满足真实、完整的标准。已有文物入口保留，未达到新标准的对象不标为本阶段已达标。最强反方是精品路线的精细体验会集中在少数对象：访客看完编钟后，想同样放大旁边另一件文物，可能发现它仍只有现有展示能力。若“沿途每件文物都能同等近看和操作”本身就是第一阶段的承诺，应把路线内全部陈列文物纳入这一轮，增加相应资料和制作工作，不能以推荐视角掩盖欠缺。

本项已确认，不重复询问。它不改变长期全展区与代表文物目标，不允许降低已承诺的空间质量，也不把用户接受覆盖范围视为已接受任何模型画面。

**D-NEXT-03：精品路线中的移动体验。** 状态 `ACCEPTED`；前置 D-NEXT-01 已接受；绑定 scope 与访客操作结果，下接 D-NEXT-02。用户回复“2，我觉得可以，选择合适的好位置让用户看，同时也可以自己看”，接受局部漫游，并明确要求推荐观看位置与自主观察并存。具体相机算法、碰撞实现与文件拆分由 Agent 负责。

R-02 增量核查：`web/components/museum-viewer.tsx` 目前使用旋转／缩放观察、按键平移相机，并根据所选条目载入单个场景；`web/app/page.tsx` 通过目录切换所选区域。代码中的平移没有行走碰撞判断，不能等同于已完成可步行路线。`web/lib/museum.ts` 已登记南广场、入口服务区、中庭和曾侯乙展厅，编钟／编磬已有资产，鸳鸯盒仍是独立试制入口。故“南广场—入口服务区—中庭—曾侯乙礼乐展厅”可作为阶段路线候选；这不证明它们已经按实馆连续动线连接，也不表示具体停靠点已经确认。

- 选项 1：分站导览。点击下一站，在各站转视角、缩放并进入文物近看。最接近已有能力，但自己逛馆的感觉较弱。
- 选项 2：局部漫游。大厅和展厅内可自行走近、转身观察；跨区域从导览点切换。能在重点空间产生漫游感，区间行进会中断。
- 选项 3：全程步行。沿真实连接关系从入口连续走到展厅。空间连续性最强，必须同时处理途中楼梯／过道、可行走地面和遮挡空间；资料及制作范围更大。

选项 2 已由用户接受：保留到展柜前自主观察的体验，同时把首阶段可行走空间限制在重点区域。推荐观看位置应易于发现、支持返回，用户也能离开这些位置自行探索；具体位置由实景资料与实际观看效果确定。最强反方是跨区域切换可能仍让用户觉得在看一组场景；可证伪失败情境为访客希望亲自从大厅走到展厅门口，却只能点击跳转，无法理解空间关系。若连续走完全程本身是核心期待，应改选 3；若当前优先方便手机点击观看和快速进入文物，则改选 1。移动选择此前影响可达视角与近看范围，故先于 D-NEXT-02 确认；现依赖已满足。以后可调整逛法，但已经为不可达区域制作的细节、连通空间与移动控制会产生成本。

R-03 增量核查：`web/lib/museum.ts` 已有园区总览／南广场、入口全景／服务台近看／下层大厅、树形中庭／旋梯近看／临窗环廊等预设视角，可复用为推荐观看位置的候选，尚无用户认定这些就是最佳位置的回执。曾侯乙展厅登记了编钟、编磬与尊盘；鸳鸯盒仍是独立试制，不能写成已经进入该展厅。本轮查看了 `web/public/renders/public-12/arrival-service.jpg` 与 `.release/trials/mandarin-duck-right.png`：前者仍有材质偏均一、环境细节稀疏的观感，后者仍有照片投影与局部形体表现的限制。这是 Agent 对既有 Blender 静帧的观察，不是浏览器运行结果、实馆相似度证明或用户验收。因此后续必须同时检查推荐镜头和自由观察位置；新标准的视觉结果仍待实际制作和人工判断。现有证据足以提出上述覆盖取舍，本轮没有为此重做公开资料搜索或修改模型。

### 已确认的下一阶段计划：精品路线与三组核心文物

以下为已由用户整体接受的 Task Contract `museum-route-v1`；唯一确认回执见“从哪里继续”。四节点路线、三组文物、参观方式、阶段顺序和验收边界均已确定。本轮按用户“把这个计划作为这个项目的一个收尾，后续我再来继续做”的要求完成计划文档。下游制作、运行、视觉验收和发布仍待后续，计划确认不代表这些结果已完成。

#### 要解决的问题与访客能得到的结果

用户反复指出场景颜色、细节与文物精度不足，整体仍有“玩具感”；仅增加模型数量不能解决这个问题。下一阶段让访客完成一条观感一致的参观路线：知道自己所在的区域，能从推荐位置看清空间，再在区域内自主走近和转身观察，进入核心文物细看与交互，退出后继续参观。

已确认数字参观顺序为：**园区南广场 → 南入口与服务区 → 南馆中庭 → 曾侯乙展厅**。四个节点均复用现有场景入口，跨节点以明确导览切换；这是一条数字参观顺序，不宣称复现了已经测绘的连续实馆步行路径。展厅定名、楼层与实物所在展位需按馆方资料复核；未核实的位置采用可辨认的数字关联展示，不伪称实物就在某个展柜。

| 节点或文物 | 这一阶段交付的可见变化 | 范围与限制 |
|---|---|---|
| 园区南广场 | 建筑主色、入口体量、地面、台阶与绿化形成一致观感；提供看清馆区关系与南入口的推荐位置 | 改善既有园区总览，以南广场及路线可见部分为精修重点；北馆、西馆的全域近看仍属后续 |
| 南入口与服务区 | 服务台、门厅、挑空、栏杆、台阶及主要照明按实景对照；走近服务台、绕行与返回都能正常观察 | 以可核实公开实景为依据；无法确认的陈设、尺寸和图案逐项标为近似 |
| 南馆中庭 | 树形结构、玻璃、旋梯、楼层关系与光照更可信；推荐视角与允许漫游的空间一致 | 楼层之间可通过导览位置切换；可行走范围由完整地面和真实可达关系确定，不要求连续走完全部楼梯 |
| 曾侯乙展厅 | 展柜、尺度、照明与文物色彩协调；从展厅能进入核心文物细看，再回到原参观位置 | 保留现有其他文物入口；未纳入精修的文物须满足所在位置可见外观的质量要求，不标为新阶段近看已达标 |
| 曾侯乙编钟 `bells` | 整套器形、悬挂架、青铜表面和可核实纹饰更细致；能观察不同钟体并运行击奏示范 | 按整套计，不能只精修一枚就宣称整套达标；槌／杆、击点与动作逐项对应来源；合成声音、示意回振明确说明 |
| 曾侯乙编磬 `chimes` | 整套磬石轮廓、厚度、悬挂关系及表面差异更清楚；逐枚观察和击奏示范 | 按整套计；不把无来源的统一纹理、音色或大幅摆动称为实物复原 |
| 彩绘鸳鸯形盒 `mandarin-duck` | 完成当前试制精修，近看两侧漆绘、头颈、盖饰与磨损；转头、独立取盖并复位 | 保留现状磨损；头部转角、取盖距离与未知内腔标为近似；关联到曾侯乙主题，精确展位未核实前不虚构 |

三组核心文物是已接受“2–3 件”的具体安排，编钟、编磬各按一套文物计。它们的玩法分别体现击奏与活动结构，可复用现有资产和动作实现。鸳鸯盒优先于另找无制作输入的第三件，因为本地已有多面开放摄影、馆方尺寸和活动结构说明；其仍未通过视觉验收。尊盘和其余候选继续保留现有入口及后续计划，不在本阶段扩成第四组精修。核心对象若无法满足质量标准，须记录缺口并重开受影响范围，不能用其他占位模型抵数。

推荐观看位置与自由探索共用同一套模型。每个节点以 2–3 个推荐位置作为起步方案，根据空间实际效果确定；提供当前区域、下一站、返回推荐位置与核心文物入口。用户能离开推荐位置自行观察，碰撞与边界只反映可行走空间，不以隐藏粗糙侧背面作为完成手段。进入细看前保存参观状态，返回时恢复位置；切换区域或退出细看应停止旧场景动作和声音。电脑承担主要局部漫游验收，现有窄屏导览、触控观察、暂停与减少动态效果能力需保留；不新增与当前目标无关的账号、多人或 VR 系统。

本阶段不把新旧馆连接空间、北馆、西馆及其他展厅扩成连续步行世界，也不一次完成 32 项新增与 16 组精修。它们仍在长期清单；现有访问入口和已发布内容保持可用。真实性沿用“现馆依据、允许标注近似”，不承诺扫描精度、全馆全部藏品或古代演奏的完整物理复原。模型及纹理交付继续包含可编辑 Blender 工程、对应网页文件、来源与修改说明。

#### 制作顺序与停止条件

以下为已确认的里程碑顺序；具体设计、文件范围和每步验证已补充在后文，制作留待用户后续继续。

1. **整理输入与差异**：复用已有来源，针对四个空间和三组文物逐项核对身份、比例、主色、实际展陈、动作与公开素材许可。官方、建筑方及用户指定的视频用于交叉观察；来源不同、光线不同的颜色不能直接混成真实色值。只补影响本轮可见结果的缺项，缺资料处在模型说明中定位到具体部位。
2. **完成质量样板**：先做南入口服务区与编钟。样板必须同时包含推荐位置、走近及侧背观察、编钟细看、一次完整击奏和复位。用实景对照与实际网页供用户评判颜色、器形、材质及动作。若仍有明显“玩具感”，修正这一切片，暂不扩批。
3. **完成路线和其余核心文物**：按获接受的标准推广到南广场、中庭与曾侯乙展厅，完成编磬、鸳鸯盒；接通导览切换、局部漫游、细看与返回。每完成一个节点，就检查推荐位置以外的可达角度，不把全路线问题积到最后。
4. **验证并交付候选版本**：重新打开当前 Blender 工程并检查贴图、部件和动作，验证实际导出模型、加载、交互、静态构建与浏览器原路径；整理整条路线的视觉验收材料。使用当前版本通过结果验收后，再按既有交付授权的适用范围处理仓库和 Pages，独立记录远端提交、工作流与线上结果。本轮计划讨论不执行这些外部动作。

不先承诺未经样板支持的工期。资料补查按已有计划先设一个不超过 2 个工作日的试查窗口；这不是自动续跑任务或已经开始计时。样板完成后以实际返工、模型体积和加载表现估计剩余制作量。缺少整件身份、主要形制、多面图或许可而无法满足合同标准时，先记录已查来源、具体缺项和下一条可行来源，再暂停受影响对象；只要有独立可执行工作，其他已确认节点可继续。关键样板未获视觉接受前不批量复制其做法。

重要风险与恢复方式：

- **实景证据不够**：现有公共空间和展位仍有近似。按具体部位验证，允许已确认的标注近似；若误差影响主要形制或核心动作，补资料或重开该范围，不把猜测写成事实。
- **照片投影导致侧背拉伸**：鸳鸯盒已有这一类精修需求；按多视角检查纹理与几何，在可达位置和动作展开后验收。单张渲染通过不能关闭这项风险。
- **材质许可影响可交付细节**：观察参考不等于可分发纹理。优先已确认开放素材；其他图只作为观察依据，制作原创近似或继续寻找授权输入，不把无许可图片打包进公开文件。
- **质量与加载冲突**：记录各模型体积、实际浏览器与首次进入表现，复用按需加载；加载成本影响使用时先定位当前瓶颈。任何降低已认可视觉标准的取舍须重开对应条款。
- **运行环境**：旧记录中浏览器验证曾受 Mac 锁屏阻止，未在本轮重试。恢复制作时先核对当前环境；不可得时保持浏览器证据待验，不用 Blender 静帧代替。此项不阻塞本轮计划文档。
- **已有工作保护**：实施前重新检查工作树和重叠 diff，按文件保留历史试制及用户改动；需要撤回时只撤回本阶段可辨认的改动或切回已有发布资产，不覆盖既有工作。

#### 验收标准与实际证据

本合同已接受。DOC 条目是本次计划交付的验收；AC-01—07 是已确认的下游制作标准，当前均未在新阶段工件上执行，不属于本次文档收尾的完成结果。每次结果附上对应文件／提交、场景、浏览器或 Blender 版本、视口、观察位置与实际操作；没有候选版本时不预填工件身份。内容改变后，只重验受影响条目，旧视觉接受不能外推到修改后的画面。

| 标准与可证伪的结果 | 最低证据、方法与实际步骤 | 当前结果与限制 |
|---|---|---|
| DOC-01：阶段目标、路线、文物名单与验收已获整体确认 | `human / decision`：用户对应 `museum-route-v1` 明确回复接受，并要求本次以计划收尾；见 E-013 | `PASS`；仅接受计划，不代表结果视觉接受 |
| DOC-02：方案有状态流、准确入口、依赖顺序、失败恢复与接续步骤 | `analysis / inspection`：对照当前源码入口和文件存在性逐项阅读“方案怎样落地”及 N-01—06 | `PASS`；已逐项检查状态流、入口、依赖与恢复方式，见 E-015 |
| DOC-03：文档链接、状态和写入边界一致 | `inspection`：定向检查本地链接／锚点、唯一合同回执、未执行 AC 状态与 `git diff --check -- PLAN.md README.md` | `PASS`；18 个本地链接／锚点及差异检查通过，见 E-015 |
| AC-01：四节点形成可完成的数字参观路线，细看返回不迷失 | `runtime / demonstration`：从南广场依次到展厅，进入三组核心文物并返回；核对所在区域、下一站、返回位置，以及切换后的动作／声音停止 | `PENDING`；尚无新阶段浏览器结果 |
| AC-02：推荐位置和自由探索都可用，地面、墙体、栏杆与展柜边界可信 | `runtime + human_review`：每区走推荐位置，再实际走近、绕行、转身并返回；覆盖可达侧背面、边缘与视线遮挡。持续移动与控制操作不出现影响定位的卡顿或无响应 | `PENDING`；当前相机平移不等于完成碰撞与步行验收 |
| AC-03：空间颜色、材料、比例与关键陈设可信 | `inspection + human_review`：四区分别提供来源对照、推荐位置和自由位置的同版网页图，标明光线／角度差异与近似；用户逐区判断主要色彩、轮廓、材质、明显穿模和拉伸是否接受 | `PENDING`；既有 Blender 静帧与 Agent 观察不是新标准接受 |
| AC-04：三组核心文物具备可近看的形制、纹饰与材质 | `inspection + runtime + human_review`：整体、正侧背、悬挂或接合部、重点纹饰，以及动作展开后逐项看；对无资料部分标近似。不得镜像不同图案或复制文字冒充有来源的个体差异 | `PENDING`；现有模型与试制并未被用户接受为达标 |
| AC-05：动作有依据，操作、暂停、复位与离开行为正确 | `contract / actual-GLB + runtime + human_review`：实际模型跑选择、播放、暂停、进度／复位等适用操作，再走网页原路径；对照编钟槌／杆示范、编磬击奏和鸳鸯盒转头／取盖。示意声音和运动清楚标注 | `PENDING`；复用测试需按当前改动重跑，自动检查不能证明历史动作完全复原 |
| AC-06：网页模型能加载，工程可编辑且部署包路径正确 | `build / package + runtime / inspection`：相关模型测试、类型检查、带仓库子路径的构建、实际文件加载；Blender 重开工程，核对贴图和动作。主浏览器实际走入馆—细看—交互—返回，窄屏检查现有导览和触控观察无回归 | `PENDING`；本轮仅核对入口，未执行新阶段构建或浏览器测试 |
| AC-07：公开交付的素材、解释和精度边界可追溯 | `analysis / inspection`：逐项核对文物身份、原图与作者、许可、变换及近似说明；复核公开包及 README／RIGHTS 与实际文件一致 | `PENDING`；已有来源可复用，新增或变更资产仍需核对 |

当前已存在的验证入口供实施接续：网页目录执行 `npm test`、`npm run typecheck`、`MUSEUM_BASE_PATH=/hubei-digital-museum npm run build`；仓库根目录执行 `python3 scripts/stage_pages.py`。模型实文件检查按 [模型制作记录](research/model-production.md) 与已有 `scripts/verify_bell_interaction.py` 等入口选择受影响范围。上述是未来验证程序，不是本轮通过回执；模型或控制行为改变时，先在相应用户行为路径建立必要的最小反馈。

#### 哪些结果需要人判断、本轮怎样完成

- **整体计划确认**：用户已明确接受本合同 `museum-route-v1`，回执唯一记在“从哪里继续”；不重复要求确认。
- **结果视觉接受（ACCEPTANCE_REQUIRED）**：实施时分“服务区＋编钟样板”和“全路线完成版”两个实际版本交付用户判断。材料包含当前网页入口、对应模型、最短操作步骤、实拍／模型对照及待看位置；分别记录颜色、形制、材质、动作和整体体验的观察与决定。只有用户能接受其期待的真实感，自动测试不能替填。当前没有新阶段候选工件，也无结果接受回执；计划确认不等于结果验收。
- **资料输入（INPUT_REQUIRED）**：当前没有只能由用户回答的实物资料问题。公开证据由 Agent 补查；若后续关键事实确实只能由馆方掌握，先形成具体询问草稿和用途，不要求用户猜测。
- **外部动作（AUTHORIZATION_REQUIRED）**：本轮实际操作为在现有 `PLAN.md` 完成计划与接续记录，在 README 增加入口，并连同必要的 `research/model-production.md` 文字记录纳入一次文档提交及推送；范围见 E-016。历史公开仓库、推送及 Pages 授权保留；进入具体交付时根据当时已确认对象、内容与授权范围判断，适用的既有授权直接沿用，不重复索取。整体计划确认本身不增加发送消息、购买或使用新资源的授权。

本轮是计划交付：用户已接受合同并明确后续再继续；本文件完成与其一致的设计、逐步实施说明和文档检查，交付接续路径后即可完成本轮讨论。DOC-01—03 是本轮完成依据。下游 AC-01—07 继续为未执行，不能因文档就绪记成通过。下游阶段以当前版本达到这些标准并取得样板和最终体验的实际视觉接受为完成条件；若进入公开交付，再独立记录 GitHub Pages 的实际上线结果。

#### R-04 调研与准备判断

本轮根据已接受的数量上限核查 `web/lib/museum.ts`、`web/lib/artifact-experiences.json`、`research/model-production.md`、`research/digitization-roadmap.md`、`RIGHTS.md` 和实际构建入口：编钟、编磬已登记在曾侯乙展厅，鸳鸯盒有多面开放摄影与本地试制但未接入该展厅；三者的证据和剩余问题适合形成有限阶段。未重新检索全馆资料；馆内实际位置与相似度仍留在制作验证中。

D-NEXT-01、02、03 全部 `ACCEPTED`，没有新的独立承重偏好需要用户补答，Frontier 为空。Readiness 为 `READY`：结果是四节点可参观与三组可细看的具体体验；范围与后续项分明；本轮文档无实施级阻塞；已知资料、许可、视觉与浏览器风险都有验证与恢复方式；接受标准可操作；人类判断与外部动作边界已区分。R-04 据此进入 `AWAITING_CONTRACT`。此为当时的准备判断；当前用户已确认，设计与实施计划见后文，制作仍未执行。


### 方案怎样落地

以下设计依据已经接受的 `museum-route-v1`，只细化实现与接续方式，保持合同范围、质量和人工验收不变。本次完成的是计划文档；这里的制作步骤留待用户后续继续。

**从素材到网页。** 来源及近似部位继续维护在 `research/` 的现有记录；Blender 工程负责几何、材质、部件与动作，公开 GLB 是由工程导出的网页资产。修正颜色、纹饰和轮廓时先回到工程或生成器，再检查导出后的材质；不能只改网页灯光掩盖源模型的问题。现有 `scripts/material_export.py`、`scripts/package_public_models.py` 与公开素材说明用于检查导出和分发边界，不为本阶段另建资产管理系统。

**从推荐位置到自由观察。** `web/lib/museum.ts` 保持区域、文物、推荐位置与说明的登记职责。`web/app/page.tsx` 管理所选区域、导览顺序、核心文物入口与返回目的地；`web/components/museum-viewer.tsx` 管理当前模型、相机、控制器和交互实例。路线直接使用 `campus`、`arrival`、`atrium`、`zeng` 这四个现有 ID。进入文物细看前保存该区域的相机位置、观察方向和视角；返回时恢复，主动选择推荐位置时再采用对应预设。存储随当前页面会话存在，暂不新增账号、后端或跨设备同步。

区域内的行走与文物旋转观察分别采用适合的相机操作。局部漫游先使用已安装 Three.js 的射线及场景网格识别地面和障碍，按真实可达范围约束高度和移动；具体地面、墙、玻璃栏杆及展柜需逐场景核对，不能只给整个模型一个大包围盒就当作完成碰撞。已有按键平移缺少碰撞判断，是需要替换的真实路径。当前只做有限重点区域与分区跳转，先不引入通用物理引擎或全馆导航网格；如果实际场景证明现有方法不能满足 AC-02，再针对失败扩展实现。

**从文物操作回到参观。** 编钟继续使用 `web/lib/bell-interaction.ts` 与 `web/lib/bell-performance.json`；编磬、鸳鸯盒复用 `web/lib/artifact-interaction.ts`、`web/lib/artifact-experiences.json` 和 `web/components/artifact-controls.tsx`。物体局部动作由可编辑工程和对应动作配置共同定义。模型载入仍通过 `web/lib/model-loader.ts`；切换时沿现有清理生命周期停止旧动作、声音和事件，释放旧资源，异步载入只允许当前所选对象更新画面。这里保留加载错误与重试入口，避免过去的“文物细看载入失败”重新变成无反馈空白。

保留现有架构的理由是本次需求已有对应的场景登记、模型载入和动作控制入口；重写网页、扩大为全程步行或新建统一动画平台均不能直接消除当前的颜色与形体问题。源模型、网页操作和视觉接受分别验证：Blender 看起来正确后仍要看实际网页，程序测试通过后仍需用户接受当前画面。

### 下次继续时按这些步骤做

本节是实施计划，全部尚未执行；状态统一见“从哪里继续”，实际验收仍只更新 AC-01—07。下面路径均相对本仓库根目录。

| 节点与依赖 | 文件入口、动作与预期产物 | 怎样验证与失败后去哪一步 |
|---|---|---|
| N-01：恢复现场与样板输入 | 先运行 `git status --short`，读取本计划与 `research/model-production.md`；核对 `scripts/refine_public_four.py`、`scripts/refine_arrival.py` 的历史工程输入和 `scripts/build_bell_interaction.py` 的原始编钟输入。结合 `research/README.md` 已有来源，为服务区与编钟列出当前可见差异及最少补图项 | 先确认输入存在、对象身份和输出位置，再运行生成器。旧备份缺失时优先定位现有 Release 工程或可编辑本地成果，必要时以公开 GLB 为起点；不能盲跑历史脚本覆盖试制。把缺项和选定源记录在制作记录中，满足 AC-07 的输入要求 |
| N-02：服务区与编钟视觉样板；依赖 N-01 | 服务区从 `scripts/refine_public_four.py` 的 arrival 分支及 `scripts/refine_arrival.py` 定位几何／材质来源；编钟先检查当前源模型与 `scripts/build_bell_interaction.py` 的部件、动作关系，再修改器形及表面。输出独立候选 `.blend`、网页模型及来源对照图，沿用 `.release/` 本地候选目录 | Blender 重开及导出检查只覆盖工件；使用推荐位置、走近及侧背面逐项对照 AC-03／04。不能用旧色彩通过记录替代新资产验证。主要轮廓或贴图仍失真时在本节点修正 |
| N-03：样板可参观；依赖 N-02 | 在 `web/lib/museum.ts`、`web/app/page.tsx` 与 `web/components/museum-viewer.tsx` 接入服务区推荐位置、局部漫游、进入编钟与返回，复用现有控制及加载路径。状态记录只服务于当前真实的参观往返 | 先在实际页面建立“走近服务台—转身—进入编钟—击奏／复位—返回”的反馈路径。移动边界及返回位置在对应公开行为测试中验证；实际浏览器验证 AC-01／02／05／06。然后把 N-02 与 N-03 同版样板交用户判断，视觉接受前不进入 N-04 |
| N-04：完成其余路线与文物；依赖样板视觉接受 | 沿 `scripts/refine_public_four.py` 的 campus／atrium 和 `scripts/build_full_museum.py`、`scripts/finish_full_galleries.py` 的展厅入口深化场景；编磬从现有模型与 `scripts/build_artifact_interactions.py` 定位，鸳鸯盒继续 `scripts/build_mandarin_duck.py` 的试制。按 `web/lib/museum.ts` 接入三组核心入口，精确实物展位未核实处使用明确的数字关联入口 | 每个节点分别走预设、自由观察、细看和返回。复用当前受影响的真实模型验证，鸳鸯盒沿 `scripts/verify_mandarin_duck.mjs` 与 `scripts/verify_mandarin_duck_blender.py` 检查。结果按 AC-01—07 记录；失败回到具体空间、模型或动作，不扩大为全馆返工 |
| N-05：整条路线候选验收；依赖 N-04 | 从 `web/` 运行既有模型／交互测试、类型检查、子路径构建，根目录运行 `scripts/stage_pages.py`；检查公开包、工程可编辑性、来源说明与 README。启动当前候选网页并记录真实地址，不默认旧的 localhost 页面就是这一版 | 实际走四节点、三组文物的完整路径与窄屏回归；提交当前版本的实景对照、自由观察路径和已知近似供用户判断。全部 AC 达到所需证据且最终体验获接受，才算制作阶段达标；构建和截图不代填用户判断 |
| N-06：届时执行已授权的交付；依赖 N-05 与当时适用授权 | 先检查实际 diff、资产许可和发布范围，精确分组提交及推送，沿 `.github/workflows/pages.yml` 观察构建／部署，按需更新 Blender Release | 分别记录提交、远端、工作流及线上当前资源；任一失败只报该层状态并修复。本次收尾不执行此节点，也不把历史 v0.13.0 的发布记录当作新阶段交付 |

N-01 是下次的第一个节点。首先要取得的是“服务区＋编钟从哪一份现有工程继续、缺哪几张决定质量的图、当前原路径如何复现”的可操作结论；不重新讨论已确认的精品路线、三组文物或逛法，也不从长期清单中另起一批制作。


### 从哪里继续

- 任务标识：`museum-next-stage`；文档修订 `shape-6`；更新日期 2026-09-08。
- 当前状态：`COMPLETE`（仅本次计划文档交付），当前节点为阶段收尾；项目后续建模没有完成。用户指定本次停在计划，恢复制作需等用户再来继续。
- 合同回执（唯一）：revision `museum-route-v1`；状态 `ACCEPTED`；确认人：用户；日期：2026-09-08；范围：已展示的四节点路线、编钟／编磬／鸳鸯盒、推荐位置与局部漫游、样板先行、交付与验收边界；来源 E-013。用户原话：“可以，把这个计划作为这个项目的一个收尾，后续我再来继续做”。合同语义未变，文档进度更新不产生新合同版本。
- 本次执行意图：完成计划、设计、步骤和接续说明，并按 E-016 把三份文档提交、推送到现有项目；不继续模型制作，不启动后台续跑或提醒。GitHub Pages 会按既有工作流对文档提交重新构建现有网页，不加入未验收模型。E-013 的后续再继续制作要求保持有效。
- 节点状态：合同 `ACCEPTED`；设计与实施计划已写入且文档检查通过；N-01—06 均为未来 `PENDING`，不在本次执行范围。需要用户决定的新偏好为无；将来的样板和最终结果视觉接受仍未取得。
- 下一动作：等待用户后续说“继续湖北省博物馆项目，按 PLAN.md 从 N-01 开始”。届时主 Agent 进入相邻的非 public 私有开发项目，先检查工作树，读取该项目的 PLAN.md 和 `research/model-production.md`，核对服务区及编钟现有工程／素材，形成首个样板的差异清单，再沿 N-02／03 制作与验证。
- 恢复前核对：适用规则、HEAD、既有 diff、源工程与输出目录、参考素材许可、实际浏览器可用性及当前候选页面。当前历史公开基线为 `5785d52`／v0.13.0；本次没有重新验证线上或修改发布状态。
- 未完成与恢复条件：本地鸳鸯盒仍有视觉精修和浏览器验收缺口，四区局部漫游与新质量标准尚未实现；按 N-01 检查实际源与环境后继续。旧锁屏状态只作为历史尝试记录，不能假定下次仍锁屏。历史生成器依赖本地备份，输入缺失时先定位可用成果，不盲目从零重建。
- 写入与交付：本次文档提交范围精确为 `PLAN.md`、`README.md`、`research/model-production.md`。模型、动作代码、照片、来源清单及运行报告继续保留为本地未发布工作；不以文档收尾宣称它们已验收或上线。文档交付的提交号、远端及 Pages 构建结果以 Git 提交历史和对应工作流为准；没有创建新的模型 Release。

工件入口：始终先读本文件；需要核对本地试制时读 [模型制作记录](research/model-production.md)，查看长期覆盖时读 [文物盘点](research/digitization-roadmap.md)，补素材前读 [来源目录](research/README.md) 与 [权利说明](RIGHTS.md)。本地 `.release/trials/` 是鸳鸯盒候选工程和视觉对照，不属于已发布或已接受的新版。

### 决定与进展记录

- E-001／历史决定导入：用户已选择“全展区＋各厅代表文物”，长期覆盖范围记为 `ACCEPTED`，不推定下一版本必须一次完成全部候选。
- E-002／历史决定导入：用户已选择“以现馆为依据，缺少资料处允许近似重建并标明”，真实性边界记为 `ACCEPTED`，不把近似当扫描精度。
- E-003／历史决定导入：用户要求有依据的交互与复杂模型爆炸展示，展示方向记为 `ACCEPTED`，具体动作仍须资料支持。
- E-004／2026-09-08，用户：调用 `shape-task` 共同讨论后续计划，本轮进入 `RESEARCHING`，原有工作保留。
- E-005／R-01，Agent：读取当前工作树、制作记录与 Shape Task 协议，形成上述项目事实、候选和单项 Frontier。没有新增公开资料查询，现有证据已足够说明本轮价值取舍；模型内部实现和新的资料抓取不影响此题，停止扩查。D-NEXT-01 尚未作答，推荐 B 仍为 `candidate`，进入 `AWAITING_DECISION`。
- E-006／2026-09-08，R-01，用户，D-NEXT-01：回复“我觉得 B 没问题”。答案对应上一轮明确的精品路线选项，依赖复核无冲突，状态 `FRONTIER → ACCEPTED`；接受范围仅为下一阶段的主要验收方向，不扩展为整个合同或发布许可。
- E-007／R-02，Agent：增量读取空间注册、场景载入和相机操作代码，确认当前是场景切换与相机观察，尚非连续可步行路线。此前尚未确定移动体验，它会改变质量检查中实际可达的位置，因此新增 D-NEXT-03 并作为唯一 Frontier，D-NEXT-02 保留待其解决。候选路线和局部漫游推荐均未接受；事实已足以解释取舍，未为此重新搜集全馆资料。
- E-008／2026-09-08，R-02，用户，D-NEXT-03：回复“2，我觉得可以，选择合适的好位置让用户看，同时也可以自己看”。对应已展示的局部漫游选项，状态 `FRONTIER → ACCEPTED`；新增推荐观看位置与自主观察并存的明确要求。未接受具体路线、全部合同或现有模型的视觉质量。
- E-009／R-03，Agent：增量核查现有预设视角、曾侯乙展厅登记和两个既有渲染图，区分可复用位置、尚未接入展厅的试制文物及待验收的视觉结果。D-NEXT-03 已满足依赖，D-NEXT-02 收敛为首阶段文物近看与完整交互的覆盖范围，并成为唯一 Frontier。建议 2–3 件核心文物保持 `candidate`；空间所有可达视角的质量要求由已有真实性与自主观察要求推导，待完整合同冻结。只更新本 Shape Record，未修改模型或开展新阶段实施。
- E-010／2026-09-08，R-03，用户，D-NEXT-02：回复“可以的”，对应上一轮“第一阶段先集中做好 2–3 件核心文物”的明确问题。依赖 D-NEXT-01 与 D-NEXT-03 仍有效，状态 `FRONTIER → ACCEPTED`；数量与集中精修的取舍已接受，未扩大为具体三组名单、路线合同或视觉结果接受。
- E-011／R-04，Agent：增量核查核心文物登记、动作说明、当前试制来源与权利边界、构建入口，完成 Frontier 重算及 Readiness。没有新增承重人类决定，Frontier 为空；生成 `museum-route-v1` 完整约定草案，进入 `AWAITING_CONTRACT`。所有下游模型与网页验收保留 `PENDING`，本轮仅更新计划。
- E-012／R-04，文档检查：`git diff --check -- PLAN.md` 返回 0；定向检查本节相对文件链接均存在，未写入本机绝对路径，三个决定为已接受且合同回执唯一，七条下游验收标准已列明。本轮未运行模型制作、网页构建、浏览器或结果视觉验收；这些层没有新增通过结论。
- E-013／2026-09-08，用户，C-01：回复“可以，把这个计划作为这个项目的一个收尾，后续我再来继续做”。对应已展示的完整 `museum-route-v1`，合同状态 `PENDING → ACCEPTED`；确认范围见唯一回执。用户同时明确本次只完成计划收尾，后续再继续制作；不将这次接受写成模型质量或线上结果接受。
- E-014／2026-09-08，Agent：补全同一文档的设计、模块职责、N-01—06 入口及依赖、历史生成器输入风险和具体恢复步骤；README 增加已确认计划入口并保留试制事实。合同语义未变，更新文档为 `shape-5`；进入文档范围的验证与收敛，未修改模型或代码。
- E-015／2026-09-08，文档验证与收敛：定向检查 18 个本地文件链接及锚点、正文源码入口、三个已接受决定、唯一已接受合同、六个未来实施节点及七条仍为 PENDING 的下游验收标准，返回 0；`git diff --check -- PLAN.md README.md` 返回 0。现场 HEAD 仍为 `5785d52`。DOC-01—03 均具备本轮证据，设计与合同范围一致，无本次文档交付的未决项，状态转为 `COMPLETE`（仅计划文档）；用户要求的模型制作、浏览器及视觉验收均留待后续，本次无提交或推送。
- E-016／2026-09-08，用户要求“放到项目里面，然后把项目收尾”。结合此前“制定相关计划，提一个 commit 然后 push 作为这个项目的收尾”的明确授权，本轮将已确认计划交付到现有公开项目。范围为 PLAN.md、README.md 与其直接引用的 research/model-production.md 三份文档；未验收试制和新阶段制作不随文档提交上线。前一轮仅本地保存的交付边界由本事件更新，合同目标与验收不变。
- E-017／2026-09-08，用户要求在非 public 目录开发，到达里程碑后再同步 public，并建立私有 GitHub 仓库。后续开发入口已迁到独立私有项目，本仓仅承接已验收公开候选；详见 DEVELOPMENT.md。已确认的精品路线与视觉验收标准保持不变。
